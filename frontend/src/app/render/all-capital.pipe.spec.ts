import { AllCapitalPipe } from './all-capital.pipe';

describe('AllCapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new AllCapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
